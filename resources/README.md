# Veltma resources

Built Veltma resources are deployed under `resources/[veltma]/` by
`scripts/sync-resources.ps1`.

The source repositories remain outside this server-data repository. Build and
fix those source repositories first, then synchronize their production output
here.
